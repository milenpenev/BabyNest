import { Heart, Image, Pencil, Trash2 } from "lucide-react";
import { useTranslation } from "react-i18next";
import type { Memory } from "../model/memory.types";
import { memoryDescription, memoryTitle } from "../utils/memoryDisplay";
import { useFamilyStore } from "../../../store/familyStore";
export default function MemoryCard({memory,onEdit,onDelete,onFavorite,compact=false}:{memory:Memory;onEdit?:(memory:Memory)=>void;onDelete?:(memory:Memory)=>void;onFavorite?:(id:string)=>void;compact?:boolean}){const{t,i18n}=useTranslation();const author=useFamilyStore(s=>s.members.find(member=>member.id===memory.createdBy));return <article className="overflow-hidden rounded-3xl border border-rose-100 bg-white shadow-sm dark:border-slate-700 dark:bg-slate-800">{memory.photos[0]?<img src={memory.photos[0].localUrl} alt="" className={`${compact?"h-36":"h-52"} w-full object-cover`}/>:<div className={`${compact?"h-24":"h-32"} flex items-center justify-center bg-gradient-to-br from-rose-100 via-violet-100 to-indigo-100 dark:from-rose-950 dark:via-violet-950 dark:to-indigo-950`}><Image className="h-8 w-8 text-violet-500"/></div>}<div className="p-4"><div className="flex items-start justify-between gap-3"><div className="min-w-0"><p className="text-xs font-semibold uppercase tracking-wide text-rose-600 dark:text-rose-300">{new Date(memory.date).toLocaleDateString(i18n.language,{day:"numeric",month:"long",year:"numeric"})}</p><h3 className="mt-1 break-words text-lg font-bold text-slate-900 dark:text-white">{memoryTitle(memory,t)}</h3></div>{onFavorite?<button onClick={()=>onFavorite(memory.id)} aria-label={t("memories.favorite")} className={`rounded-xl p-2 ${memory.favorite?"text-rose-600":"text-slate-400"}`}><Heart className={`h-5 w-5 ${memory.favorite?"fill-current":""}`}/></button>:null}</div>{!compact?<p className="mt-2 line-clamp-3 text-sm text-slate-600 dark:text-slate-300">{memoryDescription(memory,t)}</p>:null}<div className="mt-3 flex flex-wrap gap-1.5">{memory.tags.map(tag=><span key={tag} className="rounded-full bg-slate-100 px-2 py-1 text-xs text-slate-600 dark:bg-slate-700 dark:text-slate-300">#{tag}</span>)}</div><div className="mt-4 flex items-center gap-2 border-t border-slate-100 pt-3 text-xs text-slate-500 dark:border-slate-700"><span className="flex h-6 w-6 items-center justify-center rounded-full bg-indigo-100 font-bold text-indigo-700 dark:bg-indigo-950 dark:text-indigo-300">{author?.displayName.slice(0,1).toUpperCase()??"?"}</span><span>{author?.displayName??t("family.timeline.unknown")}</span>{onEdit?<button onClick={()=>onEdit(memory)} className="ml-auto rounded-lg p-2 text-indigo-600" aria-label={t("memories.edit")}><Pencil className="h-4 w-4"/></button>:null}{onDelete?<button onClick={()=>onDelete(memory)} className="rounded-lg p-2 text-rose-600" aria-label={t("memories.delete")}><Trash2 className="h-4 w-4"/></button>:null}</div></div></article>}
