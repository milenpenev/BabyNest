import{create}from"zustand";export type ConnectionState="offline"|"pending"|"syncing"|"synced"|"failed"|"conflict";interface SyncStatusStore{state:ConnectionState;pendingCount:number;failedCount:number;conflictCount:number;lastSuccessfulSync?:string;setStatus:(updates:Partial<Omit<SyncStatusStore,"setStatus">>)=>void}export const useSyncStatusStore=create<SyncStatusStore>(set=>({state:navigator.onLine?"synced":"offline",pendingCount:0,failedCount:0,conflictCount:0,setStatus:updates=>set(updates)}));
